---
const services = [
  {
    num: "01",
    name: "Проектирование пресс-форм",
    sub: "С нуля или по Вашей КД",
  },
  {
    num: "02",
    name: "Производство пресс-форм",
    sub: "BJ · SLM · 5-осевое ЧПУ · любая гнездность",
  },
  {
    num: "03",
    name: "Контрактное литье",
    sub: "Мелкая, средняя и крупная серия до 1 000 000 смыканий",
  },
  {
    num: "04",
    name: "Металлообработка",
    sub: "Инструментальные стали P20 · H13 · S136",
  },
  {
    num: "05",
    name: "Доработка и ремонт",
    sub: "Обслуживание действующей оснастки",
  },
];
---

<section class="services" id="services">
  <div class="services-header">
    <span class="eyebrow reveal">01 / список услуг</span>
    <span class="services-note reveal d1">полный цикл · без субподряда</span>
  </div>

  <div class="services-list">
    {
      services.map(({ num, name, sub }, i) => (
        <div class={`svc-row reveal${i > 0 ? ` d${i}` : ""}`}>
          <span class="svc-num">{num}</span>
          <div class="svc-name">{name}</div>
          <div class="svc-sub">{sub}</div>
        </div>
      ))
    }
  </div>

  <div class="services-float" id="svcFloat">
    <img src="/assets/photo-mold-services.png" alt="Пресс-форма" class="img-smooth" />
  </div>
</section>

<style>
  .services {
    position: relative;
    z-index: 8;
    background: var(--paper);
    overflow: hidden;
    padding: clamp(32px, 5vh, 56px) var(--pad);
    padding-left: calc(50% + var(--pad));
    height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  .services-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: clamp(16px, 2vh, 24px);
    position: relative;
    z-index: 1;
  }
  .services-note {
    font-size: 18px;
    font-weight: 500;
    letter-spacing: 0.28em;
    color: var(--grey-40);
  }
  .services-list {
    position: relative;
    z-index: 1;
  }
  .svc-row {
    display: grid;
    grid-template-columns: auto 1fr;
    column-gap: clamp(18px, 2.4vw, 36px);
    row-gap: clamp(6px, 0.8vh, 10px);
    align-items: baseline;
    padding: clamp(8px, 1vh, 12px) 0;
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    transition: padding-left 0.15s var(--ease);
    cursor: default;
  }
  .svc-row:first-child {
    border-top: 1px solid rgba(0, 0, 0, 0.08);
  }
  .svc-row:hover {
    padding-left: 12px;
  }
  .svc-num {
    grid-column: 1;
    grid-row: 1;
    font-family: var(--fd);
    font-weight: 800;
    font-size: clamp(1.25rem, min(2.2vw, 2.8svh), 2.625rem);
    letter-spacing: 0.06em;
    line-height: 1;
    color: var(--orange);
  }
  .svc-name {
    grid-column: 2;
    grid-row: 1;
    font-family: var(--fd);
    font-weight: 800;
    font-size: clamp(1.25rem, min(2.2vw, 2.8svh), 2.625rem);
    letter-spacing: -0.02em;
    text-transform: uppercase;
    color: #000;
    line-height: 1;
  }
  .svc-sub {
    grid-column: 2;
    grid-row: 2;
    font-size: var(--type-caption);
    color: var(--muted);
    margin-top: 0;
    letter-spacing: 0.03em;
  }
  .services-float {
    position: absolute;
    left: 0;
    right: calc(50% - var(--pad));
    top: 0;
    bottom: 0;
    width: auto;
    z-index: 1;
    pointer-events: none;
    display: flex;
    align-items: center;
  }
  .services-float img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: left center;
    mix-blend-mode: multiply;
  }

  @media (max-width: 767px) {
    .services {
      padding: clamp(32px, 5vh, 56px) var(--pad);
      justify-content: flex-start;
    }
    .services-float {
      display: none;
    }
  }
  @media (max-width: 767px) {
    .svc-name {
      white-space: normal;
    }
    .services-header > *:last-child {
      display: none;
    }
  }
</style>
